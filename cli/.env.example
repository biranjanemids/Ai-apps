# URL of your multi-model API server
AI_SERVER_URL=http://localhost:3000

# API key for the server (leave empty if server has no auth)
AI_API_KEY=

# Default model to use (must exist in server's models.yaml)
AI_MODEL=gpt-4o
